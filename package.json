{
  "name": "pirozki-skill-alice",
  "version": "1.0.0",
  "description": "Skill for Alice",
  "main": "src/index.js",
  "scripts": {
    "start": "node ./src/index",
    "start-cli": "node ./src/index --pirocli",
    "start-debug": "node --inspect-brk ./src/index --pirocli",
    "deploy": "now --public --no-clipboard && now alias && now rm pirozki-skill-alice --safe --yes"
  },
  "author": "SAPer",
  "license": "MIT",
  "dependencies": {
    "dashbot": "^9.9.2",
    "fast-html-parser": "^1.0.1",
    "mongodb": "^3.1.1",
    "node-fetch": "^2.1.2"
  }
}
